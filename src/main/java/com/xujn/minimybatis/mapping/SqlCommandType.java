package com.xujn.minimybatis.mapping;

/**
 * Supported SQL command types for phase 1.
 *
 * <p>Responsibility: restrict mapped statements to the minimal command set we
 * intentionally implement.
 *
 * <p>Thread-safety: enum constants are thread-safe.
 */
public enum SqlCommandType {
    SELECT
}
